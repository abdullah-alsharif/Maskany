/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/whatsapp-button.tsx` (per the T-021 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { WhatsAppFab, WhatsAppIconButton } from './property/whatsapp-button';
