/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/image-gallery.tsx` (per the T-013 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { ImageGallery } from './property/image-gallery';
