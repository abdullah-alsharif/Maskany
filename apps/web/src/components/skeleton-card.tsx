/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/skeleton-card.tsx` (per the T-012 AC)
 * resolves to the shared skeleton implementation.
 */
export { SkeletonCard } from './ui/skeleton';
