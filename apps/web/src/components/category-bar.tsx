/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/category-bar.tsx` (per the T-012 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { CategoryBar } from './property/category-bar';
