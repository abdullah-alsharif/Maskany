/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/search-bar.tsx` (per the T-016 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { SearchBar } from './property/search-bar';
