/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/favorite-button.tsx` (per the T-027 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { FavoriteButton } from './property/favorite-button';
