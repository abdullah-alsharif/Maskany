/**
 * Barrel re-export so the canonical path
 * `apps/web/src/components/property-card.tsx` (per the T-012 AC)
 * resolves to the themed implementation under `./property/`.
 */
export { PropertyCard } from './property/property-card';
